extends layout
block link
  link(rel='stylesheet' href="/stylesheets/main.css")
block subTitle
  a.btn.btn-secondary(href='/manage/emp') 직원 인사 관리
  a.btn.btn-secondary(href='#') 프로젝트 경영 관리
block content
  .cur-project.project-wrapper
    section.search_manage
      h2 프로젝트 정보
      form#form_manage(action='/manage/proj' method='get')
        input(type="search", name="proj_name" placeholder='프로젝트명 입력')
        button(type='submit')#manage_search_btn.btn.btn-primary 검색
    table.project-table
      thead
        tr
          th(scope='cols') 프로젝트명
          th(scope='cols') 프로젝트 종류
          th(scope='cols') 투입 인원
          th(scope='cols') 투입 시간
          th(scope='cols') 프로젝트 기간
      tbody 
        each project_info in projs
          tr(onclick=`location.href="/manage/proj/detail/${project_info.PRO_ID}"`)
            td=project_info.PRO_TITLE
            td=project_info.PRO_TYPE
            td=project_info.PRO_NUM
            td=project_info.SUM_WORKING_HOUR
            td=`${project_info.PRO_START_DATE} ~ ${project_info.PRO_START_DATE}`
